# Используем официальный образ Playwright с поддержкой Java и всеми зависимостями
FROM mcr.microsoft.com/playwright/java:v1.56.0-jammy

# Создаём рабочую директорию
WORKDIR /app

# Открываем порт для удалённого дебага
EXPOSE 5005

# Контейнер ожидает, что JAR будет смонтирован (через volume)
# Поэтому ничего не копируем — просто указываем команду запуска
CMD ["java", "-jar", "/app/app.jar"]
